import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    textAlign: 'center',
    fontSize: 25,
    position: 'static',
    marginTop: '40%',
  },
}));

export default useStyles;
