import styled from 'styled-components';

export default styled.div`
  .anticon-info-circle {
    color: rgba(0, 0, 0, 0.45);
  }
  .container-info-item > * {
    margin: 0 5px;
  }
  .container-content {
    padding: 20px;
    background: #fff;
  }
  .ant-space {
    width: 100%;
  }
`;
