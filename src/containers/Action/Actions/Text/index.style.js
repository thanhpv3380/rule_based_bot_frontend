import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  btnAdd: {
    width: '100%',
    textAlign: 'center',
  },
  input: {
    flexGrow: 1,
    height: '100%',
  },
}));

export default useStyles;
