import { makeStyles } from '@material-ui/styles';

export default makeStyles(() => ({
  avatar: {
    borderRadius: 10,
    height: 80,
    width: 80,
    color: '#fafafa',
    backgroundColor: '#bdbdbd',
  },
}));
